# A preset that extends standard with only a lighter color scheme.
# Since layout/fonts/defaults are inherited, overriding just the colors is enough.
name: light
extends: ./theme.yaml

colors:
  bg: "#f7f7fb"
  fg: "#1a1b26"
  accent: "#5a4cf0"
  muted: "#6a6f87"
  panel: "#ffffff"
